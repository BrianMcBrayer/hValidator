(function () {

var company = 'heroicVentures',
    module = 'validation',
    directive = 'nValidator';

angular.module(company + '.' + module, [])
.directive(directive,
            [
function () {
    
}]);

})()