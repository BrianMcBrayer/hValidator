(function () {

var company = 'heroicVentures',
    module = 'validation',
    directive = 'hValidator';

angular.module(company + '.' + module, [])
.directive(directive,
            [
function () {
    
}]);

})()